title: "Omnitryx Inc."
description: "Omnitryx Inc. is a Canadian company building businesses that create jobs, serve people, and make communities economically stronger. Technology, publishing, and more."

url: "https://omnitryx.ca"
baseurl: ""
logo: "/assets/images/logo.png"

markdown: kramdown

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-redirect-from

collections:
  services:
    output: true
    permalink: /services/:name/

include:
  - pages

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
      description: "Omnitryx Inc. is a Canadian company building businesses that create jobs, serve people, and make communities economically stronger."

  - scope:
      path: "services"
      type: "services"
    values:
      layout: "service"

  - scope:
      path: "legal"
    values:
      layout: "default"

permalink: /:title/

sass:
  style: compressed

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - README.md
  - _site
  - .gitignore
  - .DS_Store